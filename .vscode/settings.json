{
    "editor.formatOnSave": true,
    "emeraldwalk.runonsave": {
        "commands": [
            {
                "match": "^(.*\\.(ts|sh|json))$|^([^.]+)$",
                "cmd": "npm run dev"
            }
        ]
    }
}
